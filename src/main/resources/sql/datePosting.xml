<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.taky.and.wendy.date.mapper.DatePostingMapper">
	<insert id="insertDatePosting" parameterType="datePosting">
		/* DatePosting.insertDatePosting */
		INSERT INTO DatePosting (
			title,
			`condition`,
			thumbnail_path,
			body,
			regdate,
			moddate,
			editor
		) VALUES (
			#{title},
			#{condition},
			#{thumbnailPath},
			#{body},
			NOW(),
			NOW(),
			#{editor}
		)
		<selectKey keyProperty="id" resultType="long">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
</mapper>