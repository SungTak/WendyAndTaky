<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.taky.and.wendy.date.mapper.DateCoursePostMapper">
	<insert id="insertDateCoursePost" parameterType="dateCoursePost">
		/* DateCoursePost.insertDateCoursePost */
		INSERT INTO date_course_post (
			title,
			`condition`,
			region,
			thumbnail_path,
			body,
			regdate,
			moddate,
			editor
		) VALUES (
			#{title},
			#{condition},
			#{region},
			#{thumbnailPath},
			#{body},
			NOW(),
			NOW(),
			#{editor}
		)
		<selectKey keyProperty="id" resultType="long">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
</mapper>